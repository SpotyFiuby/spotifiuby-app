import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        margin: 10,
    },
    title: {
        color: "white",
        fontSize: 30,
        fontWeight: 'bold',
    },
    text: {
        marginTop: 20,
        alignSelf: 'center',
        fontSize: 20,
        color: 'white',
    },
});

export default styles;
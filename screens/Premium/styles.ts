import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
      flex: 1,
    },
    title: {
        fontSize: 27,
        fontWeight: 'bold',
        marginTop: 80,
        marginBottom: 10,
        marginLeft: 30,
        color: "white",
    },
  });
  
  export default styles;